import { AlignmentObject } from '@aoe/util/metadataModifier';

/**
 * AlignmentObject model transformations for the target URLs of educational subjects and objectives.
 * @param alignmentObject
 */
export const transformAlignmentObject = async (alignmentObject: AlignmentObject): Promise<void> => {
  // TODO: Development in progress
};
