import { AlignmentObject } from '@aoe/util/metadataModifier';

/**
 * Modifer for the target URLs of educational subjects and objectives.
 * @param alignmentObject
 */
export const modifyEducationalSubjectAndObjectiveURL = async (alignmentObject: AlignmentObject): Promise<void> => {
  // Lots of prima code here
};
