import { CleanFilenamePipe } from './clean-filename.pipe';

describe('CleanFilenamePipe', () => {
  it('create an instance', () => {
    const pipe = new CleanFilenamePipe();
    expect(pipe).toBeTruthy();
  });
});
