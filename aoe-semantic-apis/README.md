# AOE Semantic APIs

AOE service component to fetch and store semantic metadata from external APIs.

## Usage

```bash
npm install

# build
npm run build

# run
npm start

# watch mode compilation
npm run watch-ts
```

## Endpoints

Endpoints are prefixed with `/ref/api/v1`
