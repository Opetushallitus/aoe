app:
  build: ./
  volumes:
    - ./:/app
  ports:
    - 3000:3000
  environment:
    - NODE_ENV=production
  env_file: ../.env.koodisto
  command:
    sh -c 'yarn install && yarn build && yarn serve'
  restart: unless-stopped
